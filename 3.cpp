#pragma once

#include <iostream>
#include <map>
#include <math.h>

using namespace std;

inline int problem3() 
{
	auto number = 600851475143;
	const auto number_square_root = sqrt(number);

	int n[] = new array int[number - 2];
}